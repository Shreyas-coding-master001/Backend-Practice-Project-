<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit</title>
    <style>
        *{
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    color: white;
        }
        html,body{
    width: 100%;
    height: 100%;
        }
        main{
            width: 100%;
            height: 100%;
            background-color: black;
        }
        a{
            text-decoration: none;
            color: royalblue;
            padding: 20px;
        }
        form{
            display: flex;
            flex-direction: column;
            width: 20%;
            text-align: center;
            gap: 10px;
            padding: 20px;
        }
        form #FileName{
            color: rgb(61, 61, 61);
            background-color:white;

        }
        form input{
            width: 300px;
            color: black;   
        }
        form .submitForm{
            background-color: yellow;
            border: none;
            width: 60px;
            padding: 7px;
            border-radius: 10px;
        }
    </style>
</head>
<body>
    <main>
        <a href="/">Go Back</a>
        <form  method="post" action="/Edit/<%=file%>" class="Edit" onsubmit="return validateForm()">
            <input type="text" name="oldFile" value="<%=file%>" disabled="true" id="FileName"/>
            <input type="text" name="Renamed" placeholder="Enter New Name(Add old one if not new)...." required>
            <input type="submit" class="submitForm"/>
        </from>
    </main>




    <h1 id="errorMsg" style="color:red;"></h1>
<script>
function validateForm() {
    const name = document.getElementById("Renamed").value.trim();
    const appendText = document.getElementById("AppendText").value.trim();

    if (name === "" && appendText === "") {
        document.getElementById("errorMsg").innerText = "Required some values to fill!";
        return false; // stop form submit
    }else if(name === ""){
        name = file;
    }

    return true; // allow submit
}
</script>
</body>
</html>